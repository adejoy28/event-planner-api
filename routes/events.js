const routes = require('express').Router();

const events = require('../controllers/eventsController');


// routes.route('/').get(events.getAll).post(events.createContact);
// routes.route('/:id').get(events.get).put(events.updateContact).delete(events.deleteContact);

module.exports = routes;
